// Puramente informativo — nunca entra en el cálculo de coste (ver RecepcionStock.cantidadMedida).
export enum UnidadMedida {
  KG = 'KG',
  GRAMOS = 'GRAMOS',
  LITROS = 'LITROS',
  MILILITROS = 'MILILITROS',
  UNIDADES = 'UNIDADES',
}
